[build]
command = "npm run build"
publish = "dist"

#1. API ROUTES FIRST (The AI Engine)
[[redirects]]
from = "/api/*"
to = "/.netlify/functions/:splat"
status = 200
force = true

#2. FRONTEND SECOND (The Dashboard)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
